    <div class="titre">
      <a><strong><%= link_to "Rent-Sports", root_path %></strong></a>
      </div>

      <header class="navbar" data-controller="navbar">
        <div class="corps">
          <div class="Sourcelogo" data-action="click->navbar#toggleMenu">
            <div class="logomenu"></div>
            <div class="logomenu"></div>
            <div class="logomenu"></div>
          </div>
        </div>
        <ul class="menu" data-target="navbar.menu">
          <li><%= link_to "Produits", stuffs_path %></li>
          <li><%= link_to "Déconnexion", destroy_user_session_path, data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %></li>
          <li><%= link_to "Compte", new_user_session_path %></a></li>
        </ul>
      </header>

<div class="card">
  <div class="card-header">
    <div class="text-header">Fiche d'information</div>
  </div>
  <div class="card-body">

<%= simple_form_for(@stuff) do |f| %>
  <%= f.input :name %>
  <%= f.input :category_id, collection: Category.all %>
  <%= f.input :price %>
  <%= f.input :description %>
  <%= f.button :submit, 'Mettez à jour votre équipement', class: 'btn-edit'  %>
<% end %>
      </div>
  </div>
</div>
