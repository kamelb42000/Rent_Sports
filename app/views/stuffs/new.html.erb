<body>
      <header class="navbar" data-controller="navbar">
        <div class="corps">
          <div class="Sourcelogo" data-action="click->navbar#toggleMenu">
            <div class="logomenu"></div>
            <div class="logomenu"></div>
            <div class="logomenu"></div>
          </div>
        </div>
        <ul class="menu" data-target="navbar.menu">
          <li><%= link_to "Produits", stuffs_path %></li>
          <li><%= link_to "Déconnexion", destroy_user_session_path, data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %></li>
          <li><%= link_to "Compte", new_booking_path %></a></li>
        </ul>
      </header>
    <div class="titre">
      <a><strong><%= link_to "Rent-Sports", root_path %></strong></a>
      </div>
      

<div class="card">
  <div class="card-header">
    <div class="text-header">Mettez en location votre équipement</div>
  </div>
  <div class="card-body">

<%= simple_form_for(@stuff, url: stuffs_path, method: :post) do |f| %>
  <%= f.input :name %>
   <%= f.input :category_id, collection: Category.all %>
    <%= f.input :price %>

    <div>
    <%= f.label :description %>
    <div class="textarea">
    <%= f.text_area :description %>
    </div>
    </div>
     <input type="submit" class="btn-new" value="Louez votre équipement">
<% end %>
      </div>
  </div>
</div>

</body>
